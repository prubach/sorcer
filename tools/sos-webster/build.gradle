
description = 'SORCER tools :: webster'

dependencies {
    compile(group: 'org.rioproject', name: 'webster', version:rioVersion) {
        exclude(module: 'rio-start')
    }
    provided(group: 'net.jini', name: 'jsk-lib', version:riverVersion)
    provided(group: 'com.google.guava', name: 'guava', version:'16.0.1')

    provided project(':sos:sorcer-api')
    provided project(':sos:sos-util')
    provided project(':tools:sorcer-resolver')
    provided project(':sos:sorcer-spi')
}
