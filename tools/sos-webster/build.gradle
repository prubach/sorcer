
description = 'SORCER tools :: webster'
dependencies {
    compile(group: 'org.rioproject', name: 'webster', version:rioVersion) {
exclude(module: 'rio-start')
    }
    compile(group: 'net.jini', name: 'jsk-lib', version:riverVersion) {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
  /*providedCompile project(':sos:sorcer-api')
  providedCompile project(':sos:sos-util')
  providedCompile project(':tools:sorcer-resolver')
  providedCompile project(':sos:sorcer-spi')
  */
    compile project(':sos:sorcer-api')
    compile project(':sos:sos-util')
    compile project(':tools:sorcer-resolver')
    compile project(':sos:sorcer-spi')
    compile(group: 'com.google.guava', name: 'guava', version:'16.0.1') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}
