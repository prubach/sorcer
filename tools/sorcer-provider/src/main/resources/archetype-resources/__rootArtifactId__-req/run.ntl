#!/usr/bin/nsh -f

import ${package}.${providerInterface}

t1 = task("hello", sig("sayHelloWorld", ${providerInterface}.class), dataContext("Hello", input(path("in", "value"), "TESTER"), output(path("out", "value"), null)));
	