<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
        <artifactId>sos</artifactId>
        <groupId>org.sorcersoft.sorcer</groupId>
        <version>1.0-M3-SNAPSHOT</version>
    </parent>

    <artifactId>sos-env</artifactId>
    <name>SORCER OS :: env</name>
    <url>http://sorcersoft.org/</url>
    <packaging>bundle</packaging>

    <build>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <extensions>true</extensions>
                <configuration>
                    <instructions>
                        <Export-Package>
                        	sorcer.core;-split-package=merge-last,
                        	sorcer.service
                       	</Export-Package>
                        <Private-Package>
                            sorcer.org.rioproject.*,
                            sorcer.resolver,
                            sorcer.security.*,
                            sorcer.util;-split-package=merge-last,
                            sorcer.installer
                        </Private-Package>
                        <Import-Package>
                            sorcer.core;version=${project.version},
                            sorcer.service;version=${project.version},
                            org.apache.commons.io;version=2.4,
                            org.apache.commons.lang3;version=3.1,
                            org.slf4j
                        </Import-Package>
                    </instructions>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>org.sorcersoft.sorcer</groupId>
            <artifactId>sos-util</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-lang3</artifactId>
			<version>3.1</version>
		</dependency>
        <dependency>
            <groupId>commons-io</groupId>
            <artifactId>commons-io</artifactId>
            <version>2.4</version>
        </dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
		</dependency>

		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
		</dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-simple</artifactId>
			<version>${v.slf4j}</version>
			<scope>test</scope>
		</dependency>


	</dependencies>
</project>