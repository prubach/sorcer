import net.jini.jrmp.*;
import net.jini.jeri.*;
import net.jini.jeri.tcp.*;
import sorcer.core.*;
import sorcer.core.provider.autonomic.provisioner.servme.*;

sorcer.core.provider.ServiceProvider {
		name="On-demand Provisioner";
		publishedInterfaces = new String[] { "sorcer.core.provider.autonomic.provisioner.servme.AdministrableAutonomicProvisioner" };
		description = "Handles provisioning of services using Rio 4.0";
		location = "SORCER Lab 20B - CS TTU";
		spaceEnabled = false;

        // if not declared the default exporter is BasicJeriExporter
        // exporter = new JrmpExporter(0);
        // exporter = new BasicJeriExporter(TcpServerEndpoint.getInstance(0), new BasicILFactory());
        //System.getProperty("$
        properties="${iGrid.home}${/}bin/servme/configs/ondemandprovisioner-prv.properties";    
        iconName="qos.png";
}