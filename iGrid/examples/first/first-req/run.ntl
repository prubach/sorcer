#!/usr/bin/nsh -f

import com.example.sorcer.SampleService

t1 = task("hello", sig("sayHelloWorld", SampleService.class), dataContext("Hello", input(path("in", "value"), "TESTER"), output(path("out", "value"), null)));
	