import java.net.URL;
import com.sun.jini.start.ServiceDescriptor;
import com.sun.jini.config.ConfigUtil;
import sorcer.provider.boot.Booter;
import sorcer.provider.boot.SorcerDescriptorUtil;
import sorcer.provider.boot.SorcerServiceDescriptor;
import sorcer.util.ArtifactCoordinates;
import sorcer.core.SorcerEnv;

com.sun.jini.start {
	home = "${sorcer.home}";
	policy = "${java.security.policy}";
	ps = System.getProperty("path.separator");

	// webster root directories
    jiniLibPath 	 = home + "${/}lib${/}river${/}lib";
    deployPath 		 = home + "${/}deploy";
    httpData 		 = home + "${/}data";
	repo 			 = SorcerEnv.getRepoDir() + "/";

	port = 0;


    serviceDescriptors = new ServiceDescriptor[] {
        /* SORCER Webster, set to serve up 10 directories */
        // (String policy, int port, String[] roots, String address, int startPort, int endPort, boolean debug, boolean isDaemon)
        SorcerDescriptorUtil.getWebster(policy,
                                         port,
                                         new String[] { repo,
                                                        jiniLibPath,
                                                        httpData,
                                                        deployPath
                                                        }
                                         ),
        new SorcerServiceDescriptor(
 			""+new ArtifactCoordinates("org.sorcersoft.sorcer","ex1-api","11.1")
 			+" "+new ArtifactCoordinates("org.sorcersoft.sorcer","sos-platform","11.1"),
            policy,
            ConfigUtil.concat(new Object[] {
                repo, new ArtifactCoordinates("org.sorcersoft.sorcer","ex1-api","11.1"), ps,
            	repo, new ArtifactCoordinates("org.sorcersoft.sorcer","ex1-prv","11.1"), ps
            }),
	        "sorcer.ex1.provider.WhoIsItProvider1",
          	new String[] { home + "/modules/examples/ex1/ex1-prv/src/main/resources/META-INF/sorcer/whoIsIt1-prv.config" }
        ),
        new SorcerServiceDescriptor(
 			""+new ArtifactCoordinates("org.sorcersoft.sorcer","ex1-api","11.1")
 			+" "+new ArtifactCoordinates("org.sorcersoft.sorcer","sos-platform","11.1"),
            policy,
            ConfigUtil.concat(new Object[] {
                repo, new ArtifactCoordinates("org.sorcersoft.sorcer","ex1-api","11.1"), ps,
            	repo, new ArtifactCoordinates("org.sorcersoft.sorcer","ex1-prv","11.1"), ps
            }),
	        "sorcer.ex1.provider.WhoIsItProvider2",
          	new String[] { home + "/modules/examples/ex1/ex1-prv/src/main/resources/META-INF/sorcer/whoIsIt2-prv.config" }
        )
    };
}
