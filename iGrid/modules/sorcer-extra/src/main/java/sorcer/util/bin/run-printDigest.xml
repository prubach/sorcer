<?xml version="1.0"?>

<project name="print-digest" default="run" basedir=".">

	<!-- loads system environment variables -->
	<property environment="env" />

	<import file="${env.SORCER_HOME}/modules/common-run.xml" />

	<echo message="SORCER_HOME: ${sorcer.home}" />
	<echo message="WESTER: ${provider.webster.url}" />
	
	<path id="project.classpath">
		<pathelement location="${sorcer.home}/classes/" />
		<pathelement location="${sorcer.home}/lib/jgapp.jar" />
		<pathelement location="${sorcer.home}/common/jini-core.jar" />
		<pathelement location="${sorcer.home}/common/jini-ext.jar" />
		<pathelement location="${sorcer.home}/common/sun-util.jar" />
		<pathelement location="${sorcer.home}/common/serviceui-1.1.jar" />
	</path>

	<target name="run">
		<java classname="sorcer.util.PrintDigest" inputstring="${provider.webster.url}/sorcer-prv.jar" fork="yes">
			<classpath refid="project.classpath" />
			<sysproperty key="sorcer.home" value="${sorcer.home}" />
		</java>
	</target>

</project>
