<?xml version="1.0"?>

<project default="run.requestor" basedir=".">

	<!-- loads system environment variables -->
	<property environment="env" />
	<import file="${env.IGRID_HOME}/modules/common-run.xml" />
	<echo message="webster: ${provider.webster.url}" />
	<property name="webster" value="${provider.webster.url}" />

	<property name="requestor.groupId" value="org.sorcersoft.sorcer" />
	<property name="requestor.name" value="ex3-client" />
	<property name="requestor.api" value="ex2-api" />
	<property name="requestor.version" value="11.1" />

	<target name="run.requestor" depends="mvn-init">
		<java classname="${requestor.class}" fork="yes">
		 	<classpath refid="run-req.classpath"/>
		  	<sysproperty key="java.util.logging.config.file" value="${iGrid.home}/configs/sorcer.logging" />
			<sysproperty key="java.security.policy" value="../policy/worker-req.policy" />
			<sysproperty key="sorcer.exertrun.codebase" value="${requestor.groupId}:${requestor.api}:${requestor.version}" />
			<sysproperty key="sorcer.env.file" value="${iGrid.home}/configs/sorcer.env" />
			<sysproperty key="requestor.properties" value="../configs/worker-run.properties" />
			<arg value="${ex3-client.arg1}" />
			<arg value="${ex3-client.arg2}" />
			<arg value="${ex3-client.arg3}" />			
		</java>
	</target>
</project>
