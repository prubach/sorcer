import java.net.URL;
import com.sun.jini.start.ServiceDescriptor;
import com.sun.jini.config.ConfigUtil;
import sorcer.provider.boot.Booter;
import sorcer.provider.boot.SorcerDescriptorUtil;
import sorcer.provider.boot.SorcerServiceDescriptor;
import sorcer.util.ArtifactCoordinates;


com.sun.jini.start {
	home = "${iGrid.home}";
	policy = "${java.security.policy}";
	ps = System.getProperty("path.separator");

	// webster root directories
    jiniLibPath 	 = home + "${/}lib${/}river${/}lib";
    deployPath 		 = home + "${/}deploy";
    httpData 		 = home + "${/}data";
	repo = "${user.home}/.m2/repository/";

	port = Booter.getWebsterPort();


    serviceDescriptors = new ServiceDescriptor[] {
        /* SORCER Webster, set to serve up 10 directories */
        // (String policy, int port, String[] roots, String address, int startPort, int endPort, boolean debug, boolean isDaemon)
        SorcerDescriptorUtil.getWebster(policy,
                                         port,
                                         new String[] { repo
                                                        jiniLibPath,
                                                        httpData,
                                                        deployPath
                                                        },
                                         ),
        new SorcerServiceDescriptor(
 			new ArtifactCoordinates("org.dancres.blitz","blitz-dl","2.1.7")+" "+ new ArtifactCoordinates("org.dancres.blitz","blitzui","2.1.7"),
            policy,
            ConfigUtil.concat(new Object[] {
            	repo, new ArtifactCoordinates("org.dancres.blitz","blitz","2.1.7"), ps,
            	repo, new ArtifactCoordinates("org.dancres.blitz","blitzui","2.1.7"), ps,
            	repo, new ArtifactCoordinates("com.sleepycat","je","4.1.21"), ps,
            	repo, new ArtifactCoordinates("net.jini.lookup","serviceui","2.2.1"), ps,
            	repo, new ArtifactCoordinates("org.apache.river","outrigger-dl","2.2.1"), ps,
            	repo, new ArtifactCoordinates("net.jini","jsk-platform","2.2.1")
            }),
	        sorcer.ex1.provider.WhoIsItProvider1.class.getName(),
          	new String[] { home + "${/}bin${/}blitz${/}configs${/}blitz.config" }),
        )


}
