
description = 'SORCER :: caller :: config'
dependencies {
  compile project(':provider-root:examples:caller:caller-prv')
  compile project(':sos:sorcer-ui')
  compile project(':provider-root:examples:caller:caller-dl')
}

jar {
    manifest {
        attributes(
                "Implementation-Title": project.name,
                "Implementation-Version": version,
                "OAR-OperationalString" : "caller.groovy",
                "OAR-Name" : "callerBoot",
                "OAR-Version" : project.version,
                "OAR-Activation" : "Automatic")
    }
}